{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/pl5",
            "args": [ "source.pl5" ],
            "preLaunchTask": "Build - Debug",
            "cwd": "${workspaceRoot}"
        }
    ]
}
